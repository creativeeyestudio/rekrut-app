name: "rekrut"

services:
  frontend:
    build:
      context: ./
    ports:
      - "8100:8100"
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules  # éviter de monter node_modules local
